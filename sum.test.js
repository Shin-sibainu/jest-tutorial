const sum = require("./sum");

// test("test case", () => {
//   expect(sum()).toBe(3);
// });

test("first test case", () => {
  expect(sum(10, 20)).toBe(30);
});

// test("object test", () => {
//   expect(sum()).toEqual({ name: "shincode" });
// });
